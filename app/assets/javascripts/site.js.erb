
var refreshRating = function(){

  $('.rating').raty({
    starHalf: '<%= asset_path 'star-half.png' %>',
    starOff: '<%= asset_path 'star-off.png' %>',
    starOn: '<%= asset_path 'star-on.png' %>',
    scoreName: 'comment[rating]'
  });

  $('.rated').raty({
    starHalf: '<%= asset_path 'star-half.png' %>',
    starOff: '<%= asset_path 'star-off.png' %>',
    starOn: '<%= asset_path 'star-on.png' %>',
    readOnly: true,
    score: function() {
      return $(this).attr('data-score');
    }
  });
};

window.addEventListener("load", function(){
window.cookieconsent.initialise({
  "palette": {
    "popup": {
      "background": "#1d8a8a",
      "text": "#62ffaa"
    },
    "button": {
      "background": "transparent",
      "text": "#afed71",
      "border": "#afed71"
    }
  }, 
  "content": {"message":"_! K E K S E A L A R M K E K S E A L A R M !_", "dismiss": "KAPIERT!",
    "link": "KEKSEWEITERBILDUNG"
  },"theme": "classic"
})});




$(document).on('turbolinks:load ajaxSuccess', function(){

  $('.alert').delay(3000).fadeOut(2000);
  $('.notice').delay(3000).fadeOut(2000);

  refreshRating();




  $(function() {
  $('.row > #prodcont > #innerprodcont').matchHeight({
    byRow: false,
    property: 'height',
    target: null,
    remove: false
});
});

/*  $('.prodpic').mouseenter(function(){
    $(this).css("transform","scale(1.1)");
  });
  $('.prodpic').mouseleave(function(){
    $(this).css("transform","scale(1)");
  });

  $('.prodname').mouseleave(function(){
    $(this).html("<b>Hello world!</b>");
  });
*/
// elevateZoom freezes mobile devices, so only for larger screens
// elevateZoom is in vendor/assets
  if(window.screen.width>800){
   $('.img-zoom').elevateZoom({
    zoomType: "lens",
    lensShape: "round",
    lensSize: 150
  });

  }



    
});









