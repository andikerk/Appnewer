<p>views/orders/index.html.erb</p>
<p>hier sollte dann so etwas wie eine Rechnung stehen</p>
<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>order--</th>
      <th>email-- </th>
      <th>username-- </th>
      <th>productname--</th>
      <th>product.id--</th>
      <th>total--</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= User.find(order.user_id).email %></td>
        <td><%= User.find(order.user_id).username %></td>
        <td><%= Product.find(order.product_id).name %></td>
        <td><%= order.product_id %></td>
        <td><%= order.total %></td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>



